{
  "buildCommand": "cd ui && bun run build",
  "outputDirectory": "ui/dist/client",
  "installCommand": "cd ui && bun install",
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
